# ArgoCD Demo - Branching Strategy Testing

This directory is used to test branching strategies, feature branches, and pull request workflows without affecting the main project.

## Workflow

Every change is committed and pushed to this sandbox for validation before applying patterns back to the main codebase.
